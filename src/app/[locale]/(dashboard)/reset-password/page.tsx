import AnimatedPageWrapper from "@/components/global/Slider/AnimatedPageWrapper";

import ResetPasswordContent from "@/components/dashboard/Auth/ResetPassword/ResetPasswordContent";

export default function ResetPassword() {
    return (
        <AnimatedPageWrapper>
            <main>
                <ResetPasswordContent />
            </main>
        </AnimatedPageWrapper>
    );
}
